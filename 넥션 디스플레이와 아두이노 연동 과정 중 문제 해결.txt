헤더파일이 다 추가되지 않아서 그럼.

제일큰거 하나만 추가했다고 됐다고 생각했지만 사실은

헤더파일 매 기능마다 모두 추가해야됨

NexButton 기능을 쓰려면
#include <NexButton.h> 을 추가해야되고 

NexTouch 기능을 쓰려면
#include <NexTouch.h>을 추가해야되고

하지만 이 모든 걸 한번에 추가해줄 스케치->라이브러리 포함하기 -> 여기서 내가 추가한 라이브러리 클릭하면 한번에 미리 추가 다된다.

-------------------------------------------------------------------------------------------------------------------------------

다 마쳤는데도 업로드와 컴파일이 안된다면.

NexConfig 안의 Serial2 를 Serial로 바꿔주지 않아서 그렇다.

유투브 동영상에는 Serial1 을 Serial로 바꿔주라고 하는데 어느시점부터인가 소스 업데이트가 되었나보다.


그리고 컴퓨터에 연결해서 아두이노를 가동할 때에는 시리얼 모니터를 켜야지 반응할 때가 있다.
Serial.begin(115200); 시리얼 가동~ 시동걸어주는 느낌

그냥 보조배터리에 할 때에는 시리얼 모니터 자동으로 켜진셈 되는데 말이다.